import { types } from '../types/types';

const initialState = {
    notes: [],
    activate: null
};

export const notesReducer = ( state = initialState, action ) => {

    switch( action .type ) {
        
        default:
            return state;
    }

} 